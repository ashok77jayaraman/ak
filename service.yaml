apiVersion: v1
kind: Service
metadata:
  name: flask-login-app-service
spec:
  selector:
    app: flask-login-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer

